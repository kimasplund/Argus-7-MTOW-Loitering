"""ARGUS-7 propulsion models.

Submodules are imported explicitly (``from argus7.prop import engine``) rather
than re-exported eagerly here, so that adding a sibling module cannot break an
import of this one.
"""
